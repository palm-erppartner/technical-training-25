<?xml version="1.0"?>
<odoo>
  <!-- SEARCH -->
  <record id="estate_property_type_view_search" model="ir.ui.view">
    <field name="name">estate.property.type.search</field>
    <field name="model">estate.property.type</field>
    <field name="arch" type="xml">
      <search string="Search Property Types">
        <field name="name"/>
        <filter name="active" string="Active" domain="[('active','=',True)]"/>
        <filter name="archived" string="Archived" domain="[('active','=',False)]"/>
      </search>
    </field>
  </record>

  <!-- LIST -->
  <record id="estate_property_type_view_list" model="ir.ui.view">
    <field name="name">estate.property.type.list</field>
    <field name="model">estate.property.type</field>
    <field name="arch" type="xml">
      <list string="Property Types">
        <field name="name"/>
        <field name="active" class="o_column_shrink"/>
      </list>
    </field>
  </record>

  <!-- FORM -->
  <record id="estate_property_type_view_form" model="ir.ui.view">
    <field name="name">estate.property.type.form</field>
    <field name="model">estate.property.type</field>
    <field name="arch" type="xml">
      <form string="Property Type">
        <sheet>
          <group>
            <field name="name"/>
            <field name="active"/>
          </group>
        </sheet>
      </form>
    </field>
  </record>

  <!-- ACTION -->
  <record id="estate_property_type_action" model="ir.actions.act_window">
    <field name="name">Property Types</field>
    <field name="res_model">estate.property.type</field>
    <field name="view_mode">list,form</field>
    <field name="views" eval="[
      (ref('estate_property_type_view_list'), 'list'),
      (ref('estate_property_type_view_form'), 'form')
    ]"/>
    <field name="search_view_id" ref="estate_property_type_view_search"/>
  </record>
</odoo>
